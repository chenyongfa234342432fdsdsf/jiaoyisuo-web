export { releasePageContext };
import { PageContextRelease } from './releasePageContextCommon';
import type { PageContextBuiltInClient } from './types';
declare function releasePageContext<T extends Omit<PageContextBuiltInClient, 'Page'> & PageContextRelease>(pageContext: T): T & {
    Page: unknown;
};
