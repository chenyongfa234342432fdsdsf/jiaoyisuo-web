import { IBaseSendFileOptions, ChatroomMessage, TMsgType, ISendCustomMsgOptions, ISendGeoLocationMsgOptions, ISendTextMsgOptions, ISendTipMsgOptions } from './types';
export interface ChatroomMsgServiceInterface {
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 发送文本消息
     * @locale
     *
     * @locale en
     * Send text messages
     * @locale
     */
    sendTextMsg(options: ISendTextMsgOptions): Promise<ChatroomMessage>;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 发送提醒消息
     *
     * 提醒消息用于会话内的状态提醒，如进入会话时出现的欢迎消息，或者会话命中敏感词后的提示消息等等.
     * @locale
     *
     * @locale en
     * Send notification messages
     *
     * Notification messages refer to status notifications in conversations, such as the welcome message when a person joins a conversation and the prompt messages about sensitive words detection.
     * @locale
     */
    sendTipMsg(options: ISendTipMsgOptions): Promise<ChatroomMessage>;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 发送地理位置消息
     * @locale
     *
     * @locale en
     * Send location messages
     * @locale
     */
    sendGeoLocationMsg(options: ISendGeoLocationMsgOptions): Promise<ChatroomMessage>;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 发送自定义消息
     * @locale
     *
     * @locale en
     * Send custom messages
     * @locale
     */
    sendCustomMsg(options: ISendCustomMsgOptions): Promise<ChatroomMessage>;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 发送图片消息（包含上传文件的功能）
     * @locale
     *
     * @locale en
     * Send picture messages (uploading picture files supported)
     * @locale
     */
    sendImageMsg(options: IBaseSendFileOptions): Promise<ChatroomMessage>;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 发送音频消息（包含上传文件的功能）
     * @locale
     *
     * @locale en
     * Send audio messages (uploading audio files supported)
     * @locale
     */
    sendAudioMsg(options: IBaseSendFileOptions): Promise<ChatroomMessage>;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 发送视频消息（包含上传文件的功能）
     * @locale
     *
     * @locale en
     * Send video messages (uploading video files supported)
     * @locale
     */
    sendVideoMsg(options: IBaseSendFileOptions): Promise<ChatroomMessage>;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 发送其他文件消息（包含上传文件的功能）
     * @locale
     *
     * @locale en
     * Send other file messages (uploading other files supported)
     * @locale
     */
    sendFileMsg(options: IBaseSendFileOptions): Promise<ChatroomMessage>;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 查询历史消息
     * @locale
     *
     * @locale en
     * Query historical messages
     * @locale
     */
    queryMessageHistory(options: QueryMessageHistoryOptions): Promise<ChatroomMessage[]>;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 根据标签查询历史消息
     * @locale
     *
     * @locale en
     * Query historical messages by tags
     * @locale
     */
    getHistoryMsgsByTags(options: GetHistoryMsgsByTagsOptions): Promise<ChatroomMessage[]>;
}
export declare type QueryMessageHistoryOptions = {
    timetag?: number;
    limit?: number;
    reverse?: boolean;
    msgTypes?: TMsgType[];
};
export declare type GetHistoryMsgsByTagsOptions = {
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 标签
     * @locale
     */
    tags: string[];
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 消息类型
     * @locale
     */
    types?: TMsgType[];
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 开始时间 默认 0
     * @locale
     */
    fromTime?: number;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 结束时间 默认服务器当前时间
     * @locale
     */
    toTime?: number;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 数量限制，默认 100
     * @locale
     */
    limit?: number;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 是否反向 0（默认）: 正向， 1: 反向
     * @locale
     */
    reverse?: 0 | 1;
};
