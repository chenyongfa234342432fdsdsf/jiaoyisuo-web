import { IMMessage } from './MsgServiceInterface';
import { SystemMessage } from './SystemMessageServiceInterface';
/**
 * @Multi_Lang_Tag
 * @locale cn
 * 云端会话服务定义
 * @locale
 *
 * @locale en
 * @locale
 */
export interface CloudSessionServiceInterface {
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 查询云端会话列表
     * @locale
     *
     * @locale en
     * @locale
     */
    queryCloudSessionList(options: NIMEQueryCloudSessionListOptions): Promise<NIMEQueryCloudSessionListResult>;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 查询某个云端会话
     * @locale
     *
     * @locale en
     * @locale
     */
    queryCloudSession(options: NIMEQueryCloudSessionOptions): Promise<NIMECloudSession>;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 更新云端会话
     * @locale
     *
     * @locale en
     * @locale
     */
    updateCloudSession(options: NIMEUpdateCloudSessionOptions): Promise<void>;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 删除云端会话列表
     * @locale
     *
     * @locale en
     * @locale
     */
    deleteCloudSessionList(options: NIMEDeleteCloudSessionListOptions): Promise<void>;
}
export declare type NIMECloudSession = {
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * sessionId
     *
     * 例如 p2p-cs1、team-113879441
     * @locale
     *
     * @locale en
     * sessionId
     *
     * Example: p2p-cs1, team-113879441
     * @locale
     */
    sessionId: string;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 更新时间
     * @locale
     *
     * @locale en
     * Update time
     * @locale
     */
    updateTime: number;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 扩展字段，仅自己可见
     * @locale
     *
     * @locale en
     * Extension, visible only to yourself
     * @locale
     */
    ext?: string;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 上一条消息的信息
     * @locale
     *
     * @locale en
     * Previous message
     * @locale
     */
    lastMsgInfo?: {
        /**
         * @Multi_Lang_Tag
         * @locale cn
         * 上一条消息是否被撤回
         * @locale
         *
         * @locale en
         * @locale
         */
        isLastMsgRecalled: boolean;
        /**
         * @Multi_Lang_Tag
         * @locale cn
         * 上一条消息内容，isLastMsgRecalled 为 false 时存在。
         * @locale
         *
         * @locale en
         * @locale
         */
        lastMsg?: IMMessage;
        /**
         * @Multi_Lang_Tag
         * @locale cn
         * 上一条被撤回的消息通知，isLastMsgRecalled 为 true 时存在。
         * @locale
         *
         * @locale en
         * @locale
         */
        revokedMsg?: SystemMessage;
    };
};
export declare type NIMEQueryCloudSessionListOptions = {
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 与 timestamp2 组成一个时间段，SDK 内部会判断大小调整入参顺序。默认 0，代表不限制
     * @locale
     *
     * @locale en
     * @locale
     */
    timestamp1?: number;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 与 timestamp1 组成一个时间段，SDK 内部会判断大小调整入参顺序。默认 0，代表不限制
     * @locale
     *
     * @locale en
     * @locale
     */
    timestamp2?: number;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 分页大小，默认 100
     * @locale
     *
     * @locale en
     * @locale
     */
    limit?: number;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 是否需要返回 last msg。默认 true
     * @locale
     *
     * @locale en
     * @locale
     */
    includedLastMsg?: boolean;
};
export declare type NIMEQueryCloudSessionListResult = {
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 会话列表
     * @locale
     *
     * @locale en
     * @locale
     */
    sessionList: NIMECloudSession[];
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 是否下一页还有数据
     * @locale
     *
     * @locale en
     * @locale
     */
    hasMore: boolean;
};
export declare type NIMEQueryCloudSessionOptions = {
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 会话 ID
     * @locale
     *
     * @locale en
     * @locale
     */
    sessionId: string;
};
export declare type NIMEUpdateCloudSessionOptions = {
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 会话 ID
     * @locale
     *
     * @locale en
     * @locale
     */
    sessionId: string;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 扩展字段，仅自己可见
     * @locale
     *
     * @locale en
     * @locale
     */
    ext: string;
};
export declare type NIMEDeleteCloudSessionListOptions = {
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 要删除的会话 ID 的列表
     * @locale
     *
     * @locale en
     * @locale
     */
    sessionIdList: string[];
};
