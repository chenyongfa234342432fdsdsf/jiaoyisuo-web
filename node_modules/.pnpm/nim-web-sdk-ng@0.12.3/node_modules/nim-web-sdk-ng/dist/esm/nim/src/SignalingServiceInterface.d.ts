export interface SignalingServiceInterface {
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 呼叫并加入频道: 创建一个频道, 己方加入，并邀请对方加入音视频的频道
     *
     * 注: 组合接口, 等效于 create 创建频道 + join 加入频道 + invite 邀请他人进入频道
     * @locale
     *
     * @locale en
     *
     * @locale
     */
    callEx(options: NIMESignalingCallExOptions): Promise<NIMESignalingChannel>;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 接受呼叫
     *
     * 注: 与上面的 callEx 相对，接收方收到了邀请后 joinAndAccept, 等效于 accept 接受邀请 + join 加入频道
     * @locale
     *
     * @locale en
     *
     * @locale
     */
    joinAndAccept(options: NIMESignalingJoinAndAcceptOptions): Promise<NIMESignalingChannel>;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 创建频道
     * @locale
     *
     * @locale en
     *
     * @locale
     */
    create(options: NIMESignalingCreateOptions): Promise<NIMESignalingChannel>;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 关闭频道
     * @locale
     *
     * @locale en
     *
     * @locale
     */
    close(options: NIMESignalingCloseOptions): Promise<void>;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 查询频道信息
     * @locale
     *
     * @locale en
     *
     * @locale
     */
    queryInfo(options: NIMESignalingQueryInfoOptions): Promise<NIMESignalingChannel>;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 加入频道
     * @locale
     *
     * @locale en
     *
     * @locale
     */
    join(options: NIMESignalingJoinOptions): Promise<NIMESignalingChannel>;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 离开频道
     * @locale
     *
     * @locale en
     *
     * @locale
     */
    leave(options: NIMESignalingLeaveOptions): Promise<void>;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 邀请某人进入频道
     * @locale
     *
     * @locale en
     *
     * @locale
     */
    invite(options: NIMESignalingInviteOptions): Promise<void>;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 取消邀请
     * @locale
     *
     * @locale en
     *
     * @locale
     */
    cancelInvite(options: NIMESignalingCancelInviteOptions): Promise<void>;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 拒绝进入频道的邀请
     * @locale
     *
     * @locale en
     *
     * @locale
     */
    reject(options: NIMESignalingRejectOptions): Promise<void>;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 接受进入频道的邀请
     * @locale
     *
     * @locale en
     *
     * @locale
     */
    accept(options: NIMESignalingAcceptOptions): Promise<void>;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 发送自定义信令
     * @locale
     *
     * @locale en
     *
     * @locale
     */
    sendCustomCommand(options: NIMESignalingSendCustomCommandOptions): Promise<void>;
}
export interface NIMESignalingEventInterface {
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 收到频道关闭事件
     * @locale
     *
     * @locale en
     * @locale
     */
    signalingClose: [result: NIMESignalingCloseNotify];
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 收到成员加入频道事件
     * @locale
     *
     * @locale en
     * @locale
     */
    signalingJoin: [result: NIMESignalingJoinNotify];
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 收到邀请事件
     * @locale
     *
     * @locale en
     * @locale
     */
    signalingInvite: [result: NIMESignalingInviteNotify];
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 收到取消邀请事件
     * @locale
     *
     * @locale en
     * @locale
     */
    signalingCancelInvite: [result: NIMESignalingCancelInviteNotify];
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 收到拒绝邀请事件
     * @locale
     *
     * @locale en
     * @locale
     */
    signalingReject: [result: NIMESignalingRejectNotify];
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 收到接受邀请事件
     * @locale
     *
     * @locale en
     * @locale
     */
    signalingAccept: [result: NIMESignalingAcceptNotify];
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 收到退出频道事件
     * @locale
     *
     * @locale en
     * @locale
     */
    signalingLeave: [result: NIMESignalingLeaveNotify];
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 收到自定义控制指令事件
     * @locale
     *
     * @locale en
     * @locale
     */
    signalingCustomCommand: [result: NIMESignalingCustomCommandNotify];
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 初始化时同步获取到已加入的频道列表，触发此事件
     * @locale
     *
     * @locale en
     * @locale
     */
    singalingSyncChannels: [result: NIMESignalingChannel[]];
}
export declare enum NIMEEnumSignalingType {
    /**
     * 音频
     */
    audio = 1,
    /**
     * 视频
     */
    video = 2,
    /**
     * 自定义
     */
    custom = 3
}
export declare type NIMESignalingChannel = {
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 具体频道信息
     * @locale
     *
     * @locale en
     * @locale
     */
    channelInfo: NIMESignalingChannelInfo;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 频道下的成员列表
     * @locale
     *
     * @locale en
     * @locale
     */
    memberList?: NIMESignalingChannelMemberInfo[];
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 扩展的设置数据. 诸如 nertc 参数等
     * @locale
     *
     * @locale en
     * @locale
     */
    pluginSetting?: {
        /**
         * @Multi_Lang_Tag
         * @locale cn
         * G2 音视频频道的信息.
         *
         * 注: 若是使用呼叫接口 signalingCall 且传入了 nertcInfo 的参数的，则会存在
         * @locale
         *
         * @locale en
         * @locale
         */
        nertcInfo?: NIMESignalingChannelNertcInfo;
    };
};
export declare type NIMESignalingChannelNertcInfo = {
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 云信 G2-RTC 的房间名. 是在创建频道的请求中携带的
     * @locale
     *
     * @locale en
     * @locale
     */
    nertcChannelName?: string;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 云信 G2-RTC 的 token 的有效期. 在创建频道的请求时携带, 表示token的过期时间
     *
     * 注: 单位秒, 默认10分钟
     * @locale
     *
     * @locale en
     * @locale
     */
    nertcTokenTtl?: number;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 云信 G2-RTC 的 token
     *
     * 注: 开发者可以使用该 token 加入 G2 的 RTC 房间
     * @locale
     *
     * @locale en
     * @locale
     */
    nertcToken?: string;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 云信 G2-RTC 加入房间的响应内容
     *
     * 注: 这是一个 JSON 序列化字符串
     * @locale
     *
     * @locale en
     * @locale
     */
    nertcJoinRoomResponse?: string;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 对应于invite协议的错误码：成功、不在线、推送不可达
     *
     * 注: 这是一个 JSON 序列化字符串
     * @locale
     *
     * @locale en
     * @locale
     */
    callStatus?: number;
};
export declare type NIMESignalingChannelMemberInfo = {
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 受邀请者的账号 account id
     * @locale
     *
     * @locale en
     * @locale
     */
    accid: string;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 自己在频道中对应的uid，大于零有效，无效时服务器会分配随机唯一的uid
     * @locale
     *
     * @locale en
     * @locale
     */
    uid: number;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 加入时间点
     * @locale
     *
     * @locale en
     *
     * @locale
     */
    createTime: number;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 失效时间点，失效即认为离开了频道
     * @locale
     *
     * @locale en
     *
     * @locale
     */
    expireTime: number;
};
export declare type NIMESignalingChannelInfo = {
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 频道类型
     * @locale
     *
     * @locale en
     *
     * @locale
     */
    type: NIMEEnumSignalingType;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 频道名称
     * @locale
     *
     * @locale en
     *
     * @locale
     */
    name: string;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 频道id，唯一标识
     * @locale
     *
     * @locale en
     *
     * @locale
     */
    channelId: string;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 扩展字段
     * @locale
     *
     * @locale en
     *
     * @locale
     */
    ext?: string;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 创建时间点
     * @locale
     *
     * @locale en
     *
     * @locale
     */
    createTime: number;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 失效时间点
     * @locale
     *
     * @locale en
     *
     * @locale
     */
    expireTime: number;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 创建者的账号 account id
     * @locale
     *
     * @locale en
     *
     * @locale
     */
    creatorAccid: string;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 该频道是否有效
     * @locale
     *
     * @locale en
     *
     * @locale
     */
    invalid: boolean;
};
/**
 * @Multi_Lang_Tag
 * @locale cn
 * 呼叫并加入频道的入参
 * @locale
 *
 * @locale en
 *
 * @locale
 */
export declare type NIMESignalingCallExOptions = {
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 频道类型
     * @locale
     *
     * @locale en
     *
     * @locale
     */
    type: NIMEEnumSignalingType;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 频道名称.
     *
     * 注: 不填写会随机生成一个 name
     * @locale
     *
     * @locale en
     *
     * @locale
     */
    name?: string;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 扩展字段
     * @locale
     *
     * @locale en
     *
     * @locale
     */
    ext?: string;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 自己在频道中对应的uid，大于零有效，无效时服务器会分配随机唯一的uid
     * @locale
     *
     * @locale en
     *
     * @locale
     */
    uid?: string;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 受邀请者的账号 account id
     * @locale
     *
     * @locale en
     *
     * @locale
     */
    toAccid: string;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 邀请者邀请的请求id，需要邀请者填写，之后取消邀请、拒绝、接受需要复用该 requestId
     * @locale
     *
     * @locale en
     *
     * @locale
     */
    requestId: string;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 该操作的附加信息，透传给受邀请者
     * @locale
     *
     * @locale en
     *
     * @locale
     */
    attachExt?: string;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 是否存入离线消息。默认 false
     * @locale
     *
     * @locale en
     *
     * @locale
     */
    needOffline?: boolean;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 插件数据设置
     * @locale
     *
     * @locale en
     *
     * @locale
     */
    pluginSetting: {
        /**
         * @Multi_Lang_Tag
         * @locale cn
         * G2-RTC 音视频属性
         * @locale
         *
         * @locale en
         *
         * @locale
         */
        nertcInfo?: NIMESignalingCallNertcInfo;
    };
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 推送属性
     * @locale
     *
     * @locale en
     *
     * @locale
     */
    pushInfo?: NIMESignalingPushInfo;
};
export declare type NIMESignalingCallNertcInfo = {
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 云信 G2-RTC 的房间名. 是在创建频道的请求中携带的
     * @locale
     *
     * @locale en
     * @locale
     */
    nertcChannelName?: string;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 云信 G2-RTC 的 token 的有效期. 在创建频道的请求时携带, 表示token的过期时间
     *
     * 注: 单位秒, 默认10分钟
     * @locale
     *
     * @locale en
     * @locale
     */
    nertcTokenTtl?: number;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 透传给云信 G2-RTC 加入房间的请求参数
     *
     * 注: 这是一个 JSON 序列化字符串
     * @locale
     *
     * @locale en
     * @locale
     */
    nertcJoinRoomQueryParamMap?: string;
};
export declare type NIMESignalingJoinAndAcceptOptions = {
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 频道 id
     * @locale
     *
     * @locale en
     * @locale
     */
    channelId: string;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 邀请者的账号 account id
     * @locale
     *
     * @locale en
     * @locale
     */
    fromAccid: string;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 邀请者邀请的请求id，需要邀请者填写，之后取消邀请、拒绝、接受需要复用该 requestId
     * @locale
     *
     * @locale en
     * @locale
     */
    requestId: string;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 该操作的附加信息，透传给其他人
     * @locale
     *
     * @locale en
     * @locale
     */
    attachExt?: string;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 是否存入离线消息。默认 false
     * @locale
     *
     * @locale en
     * @locale
     */
    needOffline?: boolean;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 接受邀请后，自己在频道中对应的uid。大于零有效，无效时服务器会分配随机唯一的uid
     * @locale
     *
     * @locale en
     * @locale
     */
    uid?: number;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 插件数据设置
     * @locale
     *
     * @locale en
     *
     * @locale
     */
    pluginSetting: {
        /**
         * @Multi_Lang_Tag
         * @locale cn
         * G2-RTC 音视频属性
         * @locale
         *
         * @locale en
         *
         * @locale
         */
        nertcInfo?: NIMESignalingCallNertcInfo;
    };
};
/**
 * @Multi_Lang_Tag
 * @locale cn
 * 创建频道的入参
 * @locale
 *
 * @locale en
 *
 * @locale
 */
export declare type NIMESignalingCreateOptions = {
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 频道类型
     * @locale
     *
     * @locale en
     *
     * @locale
     */
    type: NIMEEnumSignalingType;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 频道名称
     *
     * 注: 不填写会随机生成一个 name
     * @locale
     *
     * @locale en
     *
     * @locale
     */
    name?: string;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 扩展字段
     * @locale
     *
     * @locale en
     *
     * @locale
     */
    ext?: string;
};
/**
 * @Multi_Lang_Tag
 * @locale cn
 * 关闭频道的入参
 * @locale
 *
 * @locale en
 *
 * @locale
 */
export declare type NIMESignalingCloseOptions = {
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 频道 id
     * @locale
     *
     * @locale en
     * @locale
     */
    channelId: string;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 该操作的附加信息，透传给其他人
     * @locale
     *
     * @locale en
     * @locale
     */
    attachExt?: string;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 是否存入离线消息。默认 false
     * @locale
     *
     * @locale en
     * @locale
     */
    needOffline?: boolean;
};
/**
 * @Multi_Lang_Tag
 * @locale cn
 * 查询频道信息的入参
 * @locale
 *
 * @locale en
 *
 * @locale
 */
export declare type NIMESignalingQueryInfoOptions = {
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 频道名
     * @locale
     *
     * @locale en
     *
     * @locale
     */
    name: string;
};
/**
 * @Multi_Lang_Tag
 * @locale cn
 * 加入频道的入参
 * @locale
 *
 * @locale en
 *
 * @locale
 */
export declare type NIMESignalingJoinOptions = {
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 频道 id
     * @locale
     *
     * @locale en
     * @locale
     */
    channelId: string;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 该操作的附加信息，透传给其他人
     * @locale
     *
     * @locale en
     * @locale
     */
    attachExt?: string;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 自己在频道中对应的uid，大于零有效，无效时服务器会分配随机唯一的uid
     * @locale
     *
     * @locale en
     * @locale
     */
    uid?: number;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 是否存入离线消息。默认 false
     * @locale
     *
     * @locale en
     * @locale
     */
    needOffline?: boolean;
};
/**
 * @Multi_Lang_Tag
 * @locale cn
 * 离开频道的入参
 * @locale
 *
 * @locale en
 *
 * @locale
 */
export declare type NIMESignalingLeaveOptions = {
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 频道 id
     * @locale
     *
     * @locale en
     * @locale
     */
    channelId: string;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 该操作的附加信息，透传给其他人
     * @locale
     *
     * @locale en
     * @locale
     */
    attachExt?: string;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 是否存入离线消息。默认 false
     * @locale
     *
     * @locale en
     * @locale
     */
    needOffline?: boolean;
};
/**
 * @Multi_Lang_Tag
 * @locale cn
 * 邀请加入频道的入参
 * @locale
 *
 * @locale en
 *
 * @locale
 */
export declare type NIMESignalingInviteOptions = {
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 频道 id
     * @locale
     *
     * @locale en
     * @locale
     */
    channelId: string;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 受邀请者的账号 account id
     * @locale
     *
     * @locale en
     * @locale
     */
    toAccid: string;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 邀请者邀请的请求id，需要邀请者填写，之后取消邀请、拒绝、接受需要复用该 requestId
     * @locale
     *
     * @locale en
     * @locale
     */
    requestId: string;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 该操作的附加信息，透传给受邀请者
     * @locale
     *
     * @locale en
     * @locale
     */
    attachExt?: string;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 是否存入离线消息。默认 false
     * @locale
     *
     * @locale en
     * @locale
     */
    needOffline?: boolean;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 推送属性
     * @locale
     *
     * @locale en
     * @locale
     */
    pushInfo?: NIMESignalingPushInfo;
};
export declare type NIMESignalingPushInfo = {
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 是否需要推送，默认false
     * @locale
     *
     * @locale en
     * @locale
     */
    needPush?: boolean;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 推送标题
     * @locale
     *
     * @locale en
     * @locale
     */
    pushTitle?: string;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 推送内容
     * @locale
     *
     * @locale en
     * @locale
     */
    pushContent: string;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 推送自定义字段
     * @locale
     *
     * @locale en
     * @locale
     */
    pushPayload?: string;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 是否计入未读计数, 默认为 true
     * @locale
     *
     * @locale en
     * @locale
     */
    needBadge?: boolean;
};
export declare type NIMESignalingCancelInviteOptions = {
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 频道 id
     * @locale
     *
     * @locale en
     * @locale
     */
    channelId: string;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 受邀请者的账号 account id
     * @locale
     *
     * @locale en
     * @locale
     */
    toAccid: string;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 邀请者邀请的请求id，需要邀请者填写，之后取消邀请、拒绝、接受需要复用该 requestId
     * @locale
     *
     * @locale en
     * @locale
     */
    requestId: string;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 该操作的附加信息，透传给受邀请者
     * @locale
     *
     * @locale en
     * @locale
     */
    attachExt?: string;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 是否存入离线消息。默认 false
     * @locale
     *
     * @locale en
     * @locale
     */
    needOffline?: boolean;
};
export declare type NIMESignalingRejectOptions = {
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 频道 id
     * @locale
     *
     * @locale en
     * @locale
     */
    channelId: string;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 邀请者的账号 account id
     * @locale
     *
     * @locale en
     * @locale
     */
    fromAccid: string;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 邀请者邀请的请求id，需要邀请者填写，之后取消邀请、拒绝、接受需要复用该 requestId
     * @locale
     *
     * @locale en
     * @locale
     */
    requestId: string;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 该操作的附加信息，透传给其他人
     * @locale
     *
     * @locale en
     * @locale
     */
    attachExt?: string;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 是否存入离线消息。默认 false
     * @locale
     *
     * @locale en
     * @locale
     */
    needOffline?: boolean;
};
export declare type NIMESignalingAcceptOptions = {
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 频道 id
     * @locale
     *
     * @locale en
     * @locale
     */
    channelId: string;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 邀请者的账号 account id
     * @locale
     *
     * @locale en
     * @locale
     */
    fromAccid: string;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 邀请者邀请的请求id，需要邀请者填写，之后取消邀请、拒绝、接受需要复用该 requestId
     * @locale
     *
     * @locale en
     * @locale
     */
    requestId: string;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 该操作的附加信息，透传给其他人
     * @locale
     *
     * @locale en
     * @locale
     */
    attachExt?: string;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 是否存入离线消息。默认 false
     * @locale
     *
     * @locale en
     * @locale
     */
    needOffline?: boolean;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 接受邀请后，是否直接加入频道。默认为 true
     * @locale
     *
     * @locale en
     * @locale
     */
    autoJoin?: boolean;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 接受邀请后，自己在频道中对应的uid。大于零有效，无效时服务器会分配随机唯一的uid
     * @locale
     *
     * @locale en
     * @locale
     */
    uid?: number;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 接受邀请后，加入频道的操作附加信息，将在加入频道通知中带给其他频道成员
     * @locale
     *
     * @locale en
     * @locale
     */
    joinAttachExt?: string;
};
export declare type NIMESignalingSendCustomCommandOptions = {
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 频道 id
     * @locale
     *
     * @locale en
     * @locale
     */
    channelId: string;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 要通知的目标账号(account id)。如果不传则通知所有人
     * @locale
     *
     * @locale en
     * @locale
     */
    toAccid?: string;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 该操作的附加信息，透传给其他人
     * @locale
     *
     * @locale en
     * @locale
     */
    attachExt?: string;
};
/**
 * @Multi_Lang_Tag
 * @locale cn
 * 频道的事件类型，包含在线，同步，离线等
 * @locale
 *
 * @locale en
 * @locale
 */
export declare enum NIMEEnumSignalingEventType {
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 1 频道关闭
     * @locale
     *
     * @locale en
     * @locale
     */
    kClose = 1,
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 2 加入频道
     * @locale
     *
     * @locale en
     * @locale
     */
    kJoin = 2,
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 3 邀请加入频道
     * @locale
     *
     * @locale en
     * @locale
     */
    kInvite = 3,
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 4 取消邀请加入频道
     * @locale
     *
     * @locale en
     * @locale
     */
    kCancelInvite = 4,
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 5 拒绝加入频道
     * @locale
     *
     * @locale en
     * @locale
     */
    kReject = 5,
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 6 接受加入频道邀请
     * @locale
     *
     * @locale en
     * @locale
     */
    kAccept = 6,
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 7 离开
     * @locale
     *
     * @locale en
     * @locale
     */
    kLeave = 7,
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 8 自定义
     * @locale
     *
     * @locale en
     * @locale
     */
    kCustom = 8
}
/**
 * @Multi_Lang_Tag
 * @locale cn
 * 信令通知的特征值定义
 * @locale
 *
 * @locale en
 * @locale
 */
export declare enum NIMEEnumSignalingNotificationFeature {
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 0 在线
     * @locale
     *
     * @locale en
     * @locale
     */
    kDefault = 0,
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 1 离线
     * @locale
     *
     * @locale en
     * @locale
     */
    kLeave = 1,
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 3 同步
     * @locale
     *
     * @locale en
     * @locale
     */
    kSelfSync = 3
}
/**
 * @Multi_Lang_Tag
 * @locale cn
 * 信令通知消息元数据定义
 * @locale
 *
 * @locale en
 * @locale
 */
export declare type NIMESignalingNotifyMeta = {
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 通知类型
     * @locale
     *
     * @locale en
     * @locale
     */
    eventType: NIMEEnumSignalingEventType;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 频道的信息
     * @locale
     *
     * @locale en
     * @locale
     */
    channelInfo: NIMESignalingChannelInfo;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 该通知的特征值
     * @locale
     *
     * @locale en
     * @locale
     */
    feature: NIMEEnumSignalingNotificationFeature;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 该操作的扩展字段
     * @locale
     *
     * @locale en
     *
     * @locale
     */
    ext?: string;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 该操作的时间戳
     * @locale
     *
     * @locale en
     *
     * @locale
     */
    time: number;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 该通知的消息 id
     * @locale
     *
     * @locale en
     *
     * @locale
     */
    msgId: string;
};
/**
 * @Multi_Lang_Tag
 * @locale cn
 * 频道关闭的通知数据
 * @locale
 *
 * @locale en
 *
 * @locale
 */
export declare type NIMESignalingCloseNotify = {
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 操作人的账号 (account id)
     * @locale
     *
     * @locale en
     *
     * @locale
     */
    fromAccid: string;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 通知的元数据
     * @locale
     *
     * @locale en
     *
     * @locale
     */
    metaData: NIMESignalingNotifyMeta;
};
/**
 * @Multi_Lang_Tag
 * @locale cn
 * 加入频道的通知数据
 * @locale
 *
 * @locale en
 *
 * @locale
 */
export declare type NIMESignalingJoinNotify = {
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 加入成员的信息，用于获得uid
     * @locale
     *
     * @locale en
     *
     * @locale
     */
    member: NIMESignalingChannelMemberInfo;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 通知的元数据
     * @locale
     *
     * @locale en
     *
     * @locale
     */
    metaData: NIMESignalingNotifyMeta;
};
/**
 * @Multi_Lang_Tag
 * @locale cn
 * 邀请加入频道的通知数据
 * @locale
 *
 * @locale en
 *
 * @locale
 */
export declare type NIMESignalingInviteNotify = {
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 操作者的账号
     * @locale
     *
     * @locale en
     *
     * @locale
     */
    fromAccid: string;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 被邀请者的账号
     * @locale
     *
     * @locale en
     *
     * @locale
     */
    toAccid: string;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 邀请者邀请的请求 id，用于被邀请者回传 requestId 作对应的回应操作
     * @locale
     *
     * @locale en
     *
     * @locale
     */
    requestId: string;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 推送信息
     * @locale
     *
     * @locale en
     *
     * @locale
     */
    pushInfo: NIMESignalingPushInfo;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 通知的元数据
     * @locale
     *
     * @locale en
     *
     * @locale
     */
    metaData: NIMESignalingNotifyMeta;
};
/**
 * @Multi_Lang_Tag
 * @locale cn
 * 取消邀请事件的通知数据
 * @locale
 *
 * @locale en
 *
 * @locale
 */
export declare type NIMESignalingCancelInviteNotify = {
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 操作者的账号
     * @locale
     *
     * @locale en
     *
     * @locale
     */
    fromAccid: string;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 被邀请者的账号
     * @locale
     *
     * @locale en
     *
     * @locale
     */
    toAccid: string;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 邀请者邀请的请求 id，用于被邀请者回传 requestId 作对应的回应操作
     * @locale
     *
     * @locale en
     *
     * @locale
     */
    requestId: string;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 通知的元数据
     * @locale
     *
     * @locale en
     *
     * @locale
     */
    metaData: NIMESignalingNotifyMeta;
};
/**
 * @Multi_Lang_Tag
 * @locale cn
 * 拒绝邀请事件通知信息
 * @locale
 *
 * @locale en
 *
 * @locale
 */
export declare type NIMESignalingRejectNotify = {
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 邀请者的账号
     * @locale
     *
     * @locale en
     *
     * @locale
     */
    toAccid: string;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 邀请者邀请的请求 id，用于被邀请者回传 requestId 作对应的回应操作
     * @locale
     *
     * @locale en
     *
     * @locale
     */
    requestId: string;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 通知的元数据
     * @locale
     *
     * @locale en
     *
     * @locale
     */
    metaData: NIMESignalingNotifyMeta;
};
/**
 * @Multi_Lang_Tag
 * @locale cn
 * 同意邀请事件通知信息
 * @locale
 *
 * @locale en
 *
 * @locale
 */
export declare type NIMESignalingAcceptNotify = {
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 邀请者的账号
     * @locale
     *
     * @locale en
     *
     * @locale
     */
    toAccid: string;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 邀请者邀请的请求 id，用于被邀请者回传 requestId 作对应的回应操作
     * @locale
     *
     * @locale en
     *
     * @locale
     */
    requestId: string;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 通知的元数据
     * @locale
     *
     * @locale en
     *
     * @locale
     */
    metaData: NIMESignalingNotifyMeta;
};
/**
 * @Multi_Lang_Tag
 * @locale cn
 * 退出频道事件通知信息
 * @locale
 *
 * @locale en
 *
 * @locale
 */
export declare type NIMESignalingLeaveNotify = {
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 操作者账号
     * @locale
     *
     * @locale en
     *
     * @locale
     */
    fromAccid: string;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 通知的元数据
     * @locale
     *
     * @locale en
     *
     * @locale
     */
    metaData: NIMESignalingNotifyMeta;
};
/**
 * @Multi_Lang_Tag
 * @locale cn
 * 自定义的控制事件通知信息
 * @locale
 *
 * @locale en
 *
 * @locale
 */
export declare type NIMESignalingCustomCommandNotify = {
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 操作者账号
     * @locale
     *
     * @locale en
     *
     * @locale
     */
    fromAccid: string;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 通知的元数据
     * @locale
     *
     * @locale en
     *
     * @locale
     */
    metaData: NIMESignalingNotifyMeta;
};
