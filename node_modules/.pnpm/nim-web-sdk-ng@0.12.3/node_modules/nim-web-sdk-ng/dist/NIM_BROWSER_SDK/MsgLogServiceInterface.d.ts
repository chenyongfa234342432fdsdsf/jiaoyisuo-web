import { EMsgType, IMMessage, TMsgScene, TMsgType } from './MsgServiceInterface';
/**
 * @Multi_Lang_Tag
 * @locale cn
 * 消息记录相关的接口定义
 * @locale
 *
 * @locale en
 * Interface definitions related to message record management
 * @locale
 */
export interface MsgLogServiceInterface {
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 批量删除服务器上某些会话的漫游消息。只支持 p2p 消息和 team 群消息，不支持 superTeam 超级群消息
     *
     * 要想让下次同步时不再接到这个会话:
     * 1. resetSessionUnreadCount 重置过这个会话的已读信息
     * 2. deleteRoamingMsgs 删除服务器漫游消息
     * @locale
     *
     * @locale en
     * Bulk delete roaming messages of certain conversations on the server.
     *
     * Note: To stop receiving this conversation the next time you sync, perform the two steps below.
     * 1. Call resetSessionUnreadCount to reset the unread count of the conversation.
     * 2. Call deleteRoamingMsgs to delete roaming messages on the server.
     * @locale
     */
    deleteRoamingMsgs(options: SessionIdListOptions): Promise<void>;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 获取云端消息的历史记录
     *
     * 该接口用于获取一段时间内的历史消息
     * @locale
     *
     * @locale en
     * Get the historical messages from the cloud.
     *
     * This interface is used for obtaining historical messages within a period.
     * @locale
     */
    getHistoryMsgs(options: GetHistoryMsgsOptions): Promise<IMMessage[]>;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 删除某个会话的云端消息历史记录
     * @locale
     *
     * @locale en
     * Delete historical messages of a conversation from the cloud.
     * @locale
     */
    clearHistoryMsgsFromServer(options: ClearHistoryMsgsFromServerOptions): Promise<{
        timetag: number;
    }>;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 云端消息全文检索
     * @locale
     *
     * @locale en
     * @locale
     */
    ftsCloudMsgLogs(options: NIMEFtsMsgLogsOptions): Promise<IMMessage[]>;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 云端消息全文检索，按会话维度进行聚合
     * @locale
     *
     * @locale en
     * @locale
     */
    ftsCloudMsgLogsAggWithSession(options: NIMEFtsCloudMsgLogsAggWithSessionOptions): Promise<IMMessage[]>;
}
/**
 * @Multi_Lang_Tag
 * @locale cn
 * 云端消息全文检索(按会话进行聚合)的接口入参定义
 * @locale
 *
 * @locale en
 * @locale
 */
export declare type NIMEFtsCloudMsgLogsAggWithSessionOptions = {
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 关键字
     * @locale
     *
     * @locale en
     * @locale
     */
    keyword: string;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 起始时间。默认0，从最早的时候开始查
     * @locale
     *
     * @locale en
     * @locale
     */
    fromTime?: number;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 截止时间。默认0，代表不限制。
     * @locale
     *
     * @locale en
     * @locale
     */
    toTime?: number;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 返回的最大会话数量限制。默认 10
     * @locale
     *
     * @locale en
     * @locale
     */
    sessionLimit?: number;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 单个会话返回的消息数量限制。默认 5
     * @locale
     *
     * @locale en
     * @locale
     */
    msglogsLimit?: number;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 查询的时间排序规则。默认 NIMEnumFtsMsgLogsOrder.DESC 按消息时间降序。可选 NIMEnumFtsMsgLogsOrder.ASC 升序
     * @locale
     *
     * @locale en
     * @locale
     */
    orderRule?: NIMEnumFtsMsgLogsOrder;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * P2P 会话过滤的列表，最大长度 20。传入会话（p2p-accid1）的 account  示例：['accid1', 'accid2', 'accid3']
     * @locale
     *
     * @locale en
     * @locale
     */
    p2pSessionList?: string[];
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 群会话过滤的列表，最大长度 20。传入群号（team-146694936），示例：['146694936', '13897']
     * @locale
     *
     * @locale en
     * @locale
     */
    teamSessionList?: string[];
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 消息发送者过滤的列表，最大长度 20。account 列表  示例：['accid1', 'accid2', 'accid3']
     * @locale
     *
     * @locale en
     * @locale
     */
    senderList?: string[];
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 消息类型过滤  示例：[EMsgType.text, EMsgType.image]
     * @locale
     *
     * @locale en
     * @locale
     */
    msgTypeList?: EMsgType[];
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 消息子类型过滤，此类型开发者在发送消息时可自定义，且格式为大于0的整数。示例：[1, 2]
     * @locale
     *
     * @locale en
     * @locale
     */
    msgSubTypeList?: number[];
};
/**
 * @Multi_Lang_Tag
 * @locale cn
 * 云端消息全文检索的入参
 * @locale
 *
 * @locale en
 * @locale
 */
export declare type NIMEFtsMsgLogsOptions = {
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 关键字
     * @locale
     *
     * @locale en
     * @locale
     */
    keyword: string;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 起始时间。默认0，从最早的时候开始查
     * @locale
     *
     * @locale en
     * @locale
     */
    fromTime?: number;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 截止时间。默认0，代表不限制。
     * @locale
     *
     * @locale en
     * @locale
     */
    toTime?: number;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 消息数量限制。默认 10
     * @locale
     *
     * @locale en
     * @locale
     */
    msglogsLimit?: number;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 查询的时间排序规则。默认 NIMEnumFtsMsgLogsOrder.DESC 按消息时间降序。可选 NIMEnumFtsMsgLogsOrder.ASC 升序
     * @locale
     *
     * @locale en
     * @locale
     */
    orderRule?: NIMEnumFtsMsgLogsOrder;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * P2P 会话过滤的列表，最大长度 20。传入会话（p2p-accid1）的 account  示例：['accid1', 'accid2', 'accid3']
     * @locale
     *
     * @locale en
     * @locale
     */
    p2pSessionList?: string[];
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 群会话过滤的列表，最大长度 20。传入群号（team-146694936），示例：['146694936', '13897']
     * @locale
     *
     * @locale en
     * @locale
     */
    teamSessionList?: string[];
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 消息发送者过滤的列表，最大长度 20。account 列表  示例：['accid1', 'accid2', 'accid3']
     * @locale
     *
     * @locale en
     * @locale
     */
    senderList?: string[];
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 消息类型过滤  示例：[EMsgType.text, EMsgType.image]
     * @locale
     *
     * @locale en
     * @locale
     */
    msgTypeList?: EMsgType[];
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 消息子类型过滤，此类型开发者在发送消息时可自定义，且格式为大于0的整数。示例：[1, 2]
     * @locale
     *
     * @locale en
     * @locale
     */
    msgSubTypeList?: number[];
};
export declare enum NIMEnumFtsMsgLogsOrder {
    ASC = 1,
    DESC = 2
}
export declare type SessionIdListOptions = {
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * sessionId 数组
     *
     * 例如 p2p-cs1、team-113879441
     * @locale
     *
     * @locale en
     * sessionId (array)
     *
     * Example: p2p-cs1, team-113879441
     * @locale
     */
    ids: string[];
};
export declare type GetHistoryMsgsOptions = {
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 场景
     * @locale
     *
     * @locale en
     * Scenes
     * @locale
     */
    scene: TMsgScene;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 聊天对象, 账号或者群id
     * @locale
     *
     * @locale en
     * The person that the user chats with, account, or group ID
     * @locale
     */
    to: string;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 开始时间戳, 精确到 ms, 默认为 0
     * @locale
     *
     * @locale en
     * Start timestamp (accurate to ms). Default value: 0
     * @locale
     */
    beginTime?: number;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 结束时间戳, 精确到 ms, 默认为服务器的当前时间
     * @locale
     *
     * @locale en
     * End timestamp (accurate to ms). Defaults value: the server's current time
     * @locale
     */
    endTime?: number;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 上次查询的最后一条消息的 idServer, 第一次不填
     * @locale
     *
     * @locale en
     * Message ID (generated by the IM server) of the last message that was searched for. Not required for the first time.
     * @locale
     */
    lastMsgId?: string;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 本次查询的消息数量限制, 最多 100 条, 默认 100 条
     * @locale
     *
     * @locale en
     * The limit (100) of the number of messages for the current query. The default value is also 100.
     * @locale
     */
    limit?: number;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 是否为反向查询，默认 false
     *
     * false 表示从 endTime 开始往前查找历史消息;
     * true 表示从 beginTime 开始往后查找历史消息
     * @locale
     *
     * @locale en
     * Whether to sort in ascending order
     *
     * false: the returned messages are sorted in descending time order.
     * true: the returned messages are sorted in ascending time order.
     * @locale
     */
    reverse?: boolean;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 是否为升序排序
     *
     * false 表示返回的消息按时间降序排序;
     * true 表示按时间升序排序
     * @locale
     *
     * @locale en
     * Whether to sort in ascending order
     *
     * false: the returned messages are sorted in descending time order.
     * true: the returned messages are sorted in ascending time order.
     * @locale
     */
    asc?: boolean;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 消息类型列表，默认全部消息类型
     * @locale
     *
     * @locale en
     * Message types. Default value: all message types
     * @locale
     */
    msgTypes?: TMsgType[];
};
export declare type ClearHistoryMsgsFromServerOptions = {
    scene: TMsgScene;
    to: string;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 扩展字段
     * @locale
     *
     * @locale en
     * Extension field
     * @locale
     */
    ext?: string;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 是否多端同步
     * @locale
     *
     * @locale en
     * Whether to enable multi-terminal synchronization
     * @locale
     */
    isSyncSelf?: boolean;
};
