export { getGlobRoots };
import type { ResolvedConfig } from 'vite';
declare type GlobRoot = {
    pkgName: null | string;
    pkgRootResolved: string;
    includePath: null | string;
};
declare function getGlobRoots(config: ResolvedConfig): Promise<GlobRoot[]>;
