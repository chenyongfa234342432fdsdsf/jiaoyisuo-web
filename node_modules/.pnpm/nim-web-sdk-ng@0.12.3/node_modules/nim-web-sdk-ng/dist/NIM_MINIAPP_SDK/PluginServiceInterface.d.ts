export interface PluginServiceInterface {
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 获取聊天室的连接地址，返回可用的连接地址列表
     *
     * 注：更推荐开发者使用 http 接口去获取可用的连接地址列表，请自行查阅服务器文档。
     * @locale
     *
     * @locale en
     *
     * @locale
     */
    getChatroomAddress(options: GetChatroomAddressOptions): Promise<string[]>;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 获取圈组的连接地址，返回可用的连接地址列表
     *
     * 注：更推荐开发者使用 http 接口去获取可用的连接地址列表，请自行查阅服务器文档。
     * @locale
     *
     * @locale en
     *
     * @locale
     */
    getQChatAddress(options?: GetQChatAddressOptions): Promise<string[]>;
}
export declare type GetChatroomAddressOptions = {
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 聊天室 id
     * @locale
     *
     * @locale en
     *
     * @locale
     */
    chatroomId: string;
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 0 代表想要获取 ipv4 的连接，1 代表 ipv6，2 代表不限制。
     *
     * 默认 0
     * @locale
     *
     * @locale en
     *
     * @locale
     */
    ipType?: number;
};
export declare type GetQChatAddressOptions = {
    /**
     * @Multi_Lang_Tag
     * @locale cn
     * 0 代表想要获取 ipv4 的连接，1 代表 ipv6，2 代表不限制。
     *
     * 默认 0
     * @locale
     *
     * @locale en
     *
     * @locale
     */
    ipType?: number;
};
