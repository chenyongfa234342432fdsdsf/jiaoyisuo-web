export { getViteDevScripts };
import type { ViteDevServer } from 'vite';
declare type PageContext = {
    _isProduction: boolean;
    _viteDevServer: null | ViteDevServer;
    _baseUrl: string;
    urlPathname: string;
};
declare function getViteDevScripts(pageContext: PageContext): Promise<string>;
