"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.findPageFiles = void 0;
const fast_glob_1 = __importDefault(require("fast-glob"));
const utils_1 = require("../utils");
async function findPageFiles(config, fileTypes) {
    const cwd = config.root;
    const timeBase = new Date().getTime();
    let pageFiles = await (0, fast_glob_1.default)(fileTypes.map((fileType) => `**/*${fileType}.${utils_1.scriptFileExtensions}`), { ignore: ['**/node_modules/**'], cwd });
    pageFiles = pageFiles.map((p) => '/' + (0, utils_1.toPosixPath)(p));
    const time = new Date().getTime() - timeBase;
    (0, utils_1.assertWarning)(time < 2 * 1000, `Finding your page files \`**/*.page.*\` took more than two seconds (${time}ms). Reach out to the vite-plugin-ssr maintainers.`, {
        onlyOnce: 'slow-page-files-search'
    });
    return pageFiles;
}
exports.findPageFiles = findPageFiles;
