"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.extractAssetsAddQuery = void 0;
const utils_1 = require("../../utils");
const query = 'extractAssets';
function extractAssetsAddQuery(id) {
    const fileExtension = (0, utils_1.getFileExtension)(id);
    (0, utils_1.assert)(fileExtension);
    if (id.includes('?')) {
        id = id.replace('?', `?${query}&`);
    }
    else {
        id = `${id}?${query}&lang.${fileExtension}`;
    }
    return id;
}
exports.extractAssetsAddQuery = extractAssetsAddQuery;
